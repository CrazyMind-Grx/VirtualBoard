
- hosts: virtualboard

  sudo: yes
  tasks:
  - name: 1. Instalar Apache
    apt: name=apache2 state=present

  - name: 2. Instalar PHP
    apt: name=libapache2-mod-php5 state=present

  - name  3. Instalar MySQL
    apt: name=mysql-server mysql-client state=present 

  - name: 4. Start Apache
    service: name=apache2 state=running enabled=yes

  - name: 5. Instalando git.
    apt: name=git state=present

  - name: 6. Clonando Repositorio desde git
    command: git clone  https://github.com/IV-2014/VirtualBoard.git

  - name: 7. Copiar php
    copy: src=../ServerConfiguration/PHP/src/ dest=/var/www/html mode=0644
